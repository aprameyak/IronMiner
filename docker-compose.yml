version: "3.9"

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  livekit:
    image: livekit/livekit-server:latest
    command: --config /etc/livekit.yaml
    ports:
      - "7880:7880"       # HTTP + WebSocket signaling
      - "7881:7881"       # RTC over TCP
      - "7882:7882/udp"   # RTC over UDP (media)
    volumes:
      - ./livekit.yaml:/etc/livekit.yaml
    depends_on:
      - redis

# The FastAPI backend is run separately with uvicorn for hot-reload during dev.
# To run everything in Docker, uncomment below and add a Dockerfile at the root.
#
#  backend:
#    build: .
#    ports:
#      - "8000:8000"
#    env_file: .env
#    volumes:
#      - ./app:/app/app
#      - ./uploads:/app/uploads
#    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
#    depends_on:
#      - livekit

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
